{
  "name": "agents-root",
  "private": true,
  "version": "0.1.0",
  "description": "Vedang's pi agent configuration repository.",
  "type": "module",
  "scripts": {
    "check": "make check",
    "check:catalog": "bun dotagents/src/check-catalog.ts",
    "format": "make format",
    "test": "make test",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --noEmit --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@earendil-works/pi-agent-core": "^0.84.2",
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@mariozechner/pi-coding-agent": "^0.57.1",
    "@types/node": "^24.12.0",
    "ajv": "^8.18.0",
    "jscpd": "^4.0.8",
    "knip": "^5.86.0",
    "playwright": "^1.50.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
